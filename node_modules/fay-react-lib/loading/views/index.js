import React from 'react';
import ReactDom from 'react-dom';
import Spin from 'fay-react-ui/spin';
import reducerName from "../reducerName";
import { connect } from 'react-redux';
import { SHOW } from '../actionType';
import '../style/index.less';

var mapStateToProps = function mapStateToProps(state) {
  return {
    loading: state[reducerName]
  };
};

export default connect(mapStateToProps)(function (_ref) {
  var loading = _ref.loading;
  var type = loading.type,
      _loading$tip = loading.tip,
      tip = _loading$tip === void 0 ? '' : _loading$tip;
  var display = type === SHOW ? 'block' : 'none';
  return ReactDom.createPortal(React.createElement("div", {
    style: {
      display: display
    },
    className: "rj-lib-loading"
  }, React.createElement(Spin, {
    tip: tip
  })), document.body);
});
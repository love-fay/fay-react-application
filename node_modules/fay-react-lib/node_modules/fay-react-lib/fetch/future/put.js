import { reqGetBrace, promise, getQueryString, reqPostBrace } from './request';
export default (function (_ref) {
  var path = _ref.path,
      data = _ref.data,
      appSn = _ref.appSn,
      contentType = _ref.contentType,
      _ref$defaultQuery = _ref.defaultQuery,
      defaultQuery = _ref$defaultQuery === void 0 ? true : _ref$defaultQuery;

  if (defaultQuery) {
    return promise(path + (typeof data === 'object' ? getQueryString(data) : data), reqGetBrace({
      method: 'PUT',
      appSn: appSn
    }));
  }

  return promise(path, reqPostBrace({
    method: 'PUT',
    params: data,
    appSn: appSn,
    contentType: contentType
  }));
});
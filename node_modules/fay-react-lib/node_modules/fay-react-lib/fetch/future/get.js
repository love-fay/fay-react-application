import { reqGetBrace, promise, getQueryString } from './request';
export default (function (_ref) {
  var path = _ref.path,
      data = _ref.data,
      appSn = _ref.appSn;
  return promise(path + (typeof data === 'object' ? getQueryString(data) : data), reqGetBrace({
    method: 'GET',
    appSn: appSn
  }));
});
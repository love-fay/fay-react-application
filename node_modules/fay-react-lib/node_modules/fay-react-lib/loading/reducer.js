import _objectSpread from "@babel/runtime-corejs2/helpers/objectSpread";

var _reducerName;

import * as types from './actionType';
import reducerName from './reducerName';
var initState = {
  type: types.HIDE
};

var reducer = function reducer(state, action) {
  if (state === void 0) {
    state = initState;
  }

  var type = action.type;

  switch (type) {
    case types.SHOW:
      {
        return _objectSpread({}, state, {
          type: type,
          tip: action.tip
        });
      }

    case types.HIDE:
      {
        return _objectSpread({}, state, {
          type: type,
          tip: undefined
        });
      }

    default:
      {
        return state;
      }
  }
};

export default (_reducerName = {}, _reducerName[reducerName] = reducer, _reducerName);
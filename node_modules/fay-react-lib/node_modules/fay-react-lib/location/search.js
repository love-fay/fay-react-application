import _JSON$stringify from "@babel/runtime-corejs2/core-js/json/stringify";
import _objectSpread from "@babel/runtime-corejs2/helpers/objectSpread";
export var add = function add(location, data, remember) {
  if (remember === void 0) {
    remember = true;
  }

  return _objectSpread({}, location, {
    search: '?search=' + encodeURIComponent(_JSON$stringify(remember ? _objectSpread({}, get(location) || {}, data) : data))
  });
};
export var get = function get(_ref) {
  var search = _ref.search;

  if (search && search.startsWith('?search=')) {
    return JSON.parse(decodeURIComponent(search.split('?search=')[1]));
  }

  return null;
};
import _objectSpread from "@babel/runtime-corejs2/helpers/objectSpread";
import React from 'react';
import PropTypes from 'prop-types';
import AntdIcon from "../res/antd/icon";
import RjdIcon from "../res/own/icon";
import data from './data';

var Icon = function Icon(props) {
  return props.antd || data[props.type] ? React.createElement(AntdIcon, _objectSpread({
    style: {
      fontWeight: props.bold ? 'bold' : '100'
    }
  }, props, {
    bold: undefined
  })) : React.createElement(RjdIcon, props);
};

Icon.propTypes = {
  type: PropTypes.string.isRequired,
  className: PropTypes.any,
  style: PropTypes.any,
  antd: PropTypes.bool,
  bold: PropTypes.bool
};
export default Icon;
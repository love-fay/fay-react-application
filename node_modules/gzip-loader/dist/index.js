'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _zlib = require('zlib');

function loader(buf) {
  var callback = this.async();
  this.cacheable(true);
  (0, _zlib.gunzip)(buf, function (err, unzipped) {
    if (err) {
      callback(err);
    } else {
      callback(err, unzipped);
    }
  });
}

loader.raw = true;

exports.default = loader;